interface:
  display_name: "Claude Review"
  short_description: "Run Claude as an independent read-only PR gate"
  default_prompt: "Use $claude-review to review the current branch as an independent read-only external gate."
